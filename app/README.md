# App

Entry point for running the finished pipeline outside a notebook — the CLI described in the challenge spec (predict category / intent / recommendations from a raw search query).

Not implemented yet: depends on trained artifacts from `src/models` existing first.
