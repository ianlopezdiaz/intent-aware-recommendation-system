# Models

Serialized trained model artifacts (classifier, intent model, recommender indices), produced by the notebooks in `notebooks/`.

Artifact files are gitignored (see `.gitignore`) — only this README is tracked.
